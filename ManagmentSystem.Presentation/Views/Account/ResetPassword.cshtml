@model ManagmentSystem.Presentation.Models.AccountVM.ForgotPasswordVM
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<SharedResources> Localizer

@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f4f4f4;
            text-align: center;
        }

        .page-container {
            width: 100%;
            max-width: 400px;
        }

        .reset-password-container {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            position: relative;
            margin-top: -60px; /* Adjusted to overlay image */
            z-index: 1; /* Ensures it stays on top of the image */
        }

            .reset-password-container h1 {
                font-size: 24px;
                color: #333;
                margin-bottom: 20px;
            }

            .reset-password-container label {
                display: block;
                margin-bottom: 5px;
                font-weight: bold;
                color: #555;
            }

            .reset-password-container input {
                width: calc(100% - 20px);
                padding: 10px;
                margin-bottom: 15px;
                border: 1px solid #ccc;
                border-radius: 4px;
                box-sizing: border-box;
            }

        /* Validasyonla ilişkili CSS sınıfları */
        .input-validation-error {
            border-color: red;
            background-color: #fdd;
        }

        .field-validation-error {
            color: red;
            font-size: 12px;
        }

        .field-validation-valid {
            display: none;
        }

        .input-container {
            position: relative;
            width: 100%;
        }

        .show-password-icon {
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: #007bff;
        }

        button {
            width: 100%;
            background-color: #007bff;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            margin-top: 10px;
        }

            button:hover {
                background-color: #0056b3;
            }

        .header-image {
            width: 100%;
            max-height: 300px; /* Adjusted height for better view */
            object-fit: cover;
            border-radius: 8px;
            margin-bottom: -60px; /* Pulls content up to cover image */
        }
    </style>
</head>
<body>
    <div class="page-container">
        <div class="reset-password-container">
            <h1>Reset Your Password</h1>
            <form id="resetPasswordForm" asp-action="ResetPassword" method="post">
                <input type="hidden" asp-for="Token" />
                <input type="hidden" asp-for="Email" />

                <div class="input-container">
                    <label for="password">New Password:</label>
                    <input type="password" id="password" asp-for="Password" placeholder="Enter your new password" required />
                    <span class="show-password-icon" onclick="togglePasswordVisibility('password')">🔒</span>
                    <span asp-validation-for="Password" class="field-validation-error">tyhrtheth</span> <!-- Validasyon mesajı -->
                </div>

                <div class="input-container">
                    <label for="confirmPassword">Confirm New Password:</label>
                    <input type="password" id="confirmPassword" asp-for="ConfirmPassword" placeholder="Re-enter your new password" required />
                    <span class="show-password-icon" onclick="togglePasswordVisibility('confirmPassword')">🔒</span>
                    <span asp-validation-for="ConfirmPassword" class="field-validation-error">selam</span> <!-- Validasyon mesajı -->
                </div>

                <div class="field-validation-error" id="passwordMismatchError" style="display:none;">@Localizer["Passwords do not match!"]</div> <!-- Şifreler aynı değil uyarısı -->

                <button type="submit" class="btn btn-success">@Localizer["Create New Password and Back to Login"]</button>
            </form>
        </div>
    </div>

    <script>
        function togglePasswordVisibility(id) {
            const passwordField = document.getElementById(id);
            const icon = document.querySelector(`#${id} + .show-password-icon`);
            if (passwordField.type === 'password') {
                passwordField.type = 'text';
                icon.textContent = '🔓'; // Unlock icon
            } else {
                passwordField.type = 'password';
                icon.textContent = '🔒'; // Lock icon
            }
        }

        document.getElementById('resetPasswordForm').addEventListener('submit', function (event) {
            const password = document.getElementById('password').value;
            const confirmPassword = document.getElementById('confirmPassword').value;

            if (password !== confirmPassword) {
                event.preventDefault(); // Formu göndermeyi engelle
                document.getElementById('passwordMismatchError').style.display = 'block'; // Uyarıyı göster
            }
        });
    </script>

    <script src="https://ajax.aspnetcdn.com/ajax/jquery/jquery-3.6.0.min.js"></script>
    <script src="https://ajax.aspnetcdn.com/ajax/jquery.validate/1.19.3/jquery.validate.min.js"></script>
    <script src="https://ajax.aspnetcdn.com/ajax/jquery.validate.unobtrusive/3.2.12/jquery.validate.unobtrusive.min.js"></script>

</body>
</html>

@* @model ManagmentSystem.Presentation.Models.AccountVM.ForgotPasswordVM
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<SharedResources> Localizer

@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f4f4f4;
            text-align: center;
        }

        .page-container {
            width: 100%;
            max-width: 400px;
        }

        .reset-password-container {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            position: relative;
            margin-top: -60px; /* Adjusted to overlay image */
            z-index: 1; /* Ensures it stays on top of the image */
        }

            .reset-password-container h1 {
                font-size: 24px;
                color: #333;
                margin-bottom: 20px;
            }

            .reset-password-container label {
                display: block;
                margin-bottom: 5px;
                font-weight: bold;
                color: #555;
            }

            .reset-password-container input {
                width: calc(100% - 20px);
                padding: 10px;
                margin-bottom: 15px;
                border: 1px solid #ccc;
                border-radius: 4px;
                box-sizing: border-box;
            }

        /* Validasyonla ilişkili CSS sınıfları */
        .input-validation-error {
            border-color: red;
            background-color: #fdd;
        }

        .field-validation-error {
            color: red;
            font-size: 12px;
        }

        .field-validation-valid {
            display: none;
        }

        .input-container {
            position: relative;
            width: 100%;
        }

        .show-password-icon {
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: #007bff;
        }

        button {
            width: 100%;
            background-color: #007bff;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            margin-top: 10px;
        }

            button:hover {
                background-color: #0056b3;
            }

        .header-image {
            width: 100%;
            max-height: 300px; /* Adjusted height for better view */
            object-fit: cover;
            border-radius: 8px;
            margin-bottom: -60px; /* Pulls content up to cover image */
        }
    </style>
</head>
<body>
    <div class="page-container">
        <div class="reset-password-container">
            <h1>Reset Your Password</h1>
            <form asp-action="ResetPassword" method="post">
                <input type="hidden" asp-for="Token" />
                <input type="hidden" asp-for="Email" />

                <div class="input-container">
                    <label for="password">New Password:</label>
                    <input type="password" id="password" asp-for="Password" placeholder="Enter your new password" required />
                    <span class="show-password-icon" onclick="togglePasswordVisibility('password')">🔒</span>
                    <span asp-validation-for="Password" class="field-validation-error"></span> <!-- Validasyon mesajı -->
                </div>

                <div class="input-container">
                    <label for="confirmPassword">Confirm New Password:</label>
                    <input type="password" id="confirmPassword" asp-for="ConfirmPassword" placeholder="Re-enter your new password" required />
                    <span class="show-password-icon" onclick="togglePasswordVisibility('confirmPassword')">🔒</span>
                    <span asp-validation-for="ConfirmPassword" class="field-validation-error"></span> <!-- Validasyon mesajı -->
                </div>

                <button type="submit" class="btn btn-success">@Localizer["Create New Password and Back to Login"]</button>
            </form>
        </div>
    </div>

    <script>
        function togglePasswordVisibility(id) {
            const passwordField = document.getElementById(id);
            const icon = document.querySelector(`#${id} + .show-password-icon`);
            if (passwordField.type === 'password') {
                passwordField.type = 'text';
                icon.textContent = '🔓'; // Unlock icon
            } else {
                passwordField.type = 'password';
                icon.textContent = '🔒'; // Lock icon
            }
        }
    </script>

    <script src="https://ajax.aspnetcdn.com/ajax/jquery/jquery-3.6.0.min.js"></script>
    <script src="https://ajax.aspnetcdn.com/ajax/jquery.validate/1.19.3/jquery.validate.min.js"></script>
    <script src="https://ajax.aspnetcdn.com/ajax/jquery.validate.unobtrusive/3.2.12/jquery.validate.unobtrusive.min.js"></script>

</body>
</html> *@

@* @model ManagmentSystem.Presentation.Models.AccountVM.ForgotPasswordVM
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<SharedResources> Localizer

@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f4f4f4;
            text-align: center;
        }

        .page-container {
            width: 100%;
            max-width: 400px;
        }

        .reset-password-container {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            position: relative;
            margin-top: -60px; /* Adjusted to overlay image */
            z-index: 1; /* Ensures it stays on top of the image */
        }

            .reset-password-container h1 {
                font-size: 24px;
                color: #333;
                margin-bottom: 20px;
            }

            .reset-password-container label {
                display: block;
                margin-bottom: 5px;
                font-weight: bold;
                color: #555;
            }

            .reset-password-container input {
                width: calc(100% - 20px);
                padding: 10px;
                margin-bottom: 15px;
                border: 1px solid #ccc;
                border-radius: 4px;
                box-sizing: border-box;
            }

        .input-container {
            position: relative;
            width: 100%;
        }

        .show-password-icon {
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: #007bff;
        }

        button {
            width: 100%;
            background-color: #007bff;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            margin-top: 10px;
        }

            button:hover {
                background-color: #0056b3;
            }

        .header-image {
            width: 100%;
            max-height: 300px; /* Adjusted height for better view */
            object-fit: cover;
            border-radius: 8px;
            margin-bottom: -60px; /* Pulls content up to cover image */
        }
    </style>
</head>
<body>
    <div class="page-container">


        <div class="reset-password-container">
            <h1>Reset Your Password</h1>
            <form asp-action="ResetPassword" method="post">
                <input type="hidden" asp-for="Token" />
                <input type="hidden" asp-for="Email" />

                <div class="input-container">
                    <label for="password">New Password:</label>
                    <input type="password" id="password" asp-for="Password" placeholder="Enter your new password" required />
                    <span class="show-password-icon" onclick="togglePasswordVisibility('password')">🔒</span>
                </div>

                <div class="input-container">
                    <label for="confirmPassword">Confirm New Password:</label>
                    <input type="password" id="confirmPassword" asp-for="ConfirmPassword" placeholder="Re-enter your new password" required />
                    <span class="show-password-icon" onclick="togglePasswordVisibility('confirmPassword')">🔒</span>
                </div>

                <button type="submit" class="btn btn-success" onclick="window.location.href='@Url.Action("Login", "Account")'">@Localizer["Create New Password and Back to Login"]</button>
            </form>
        </div>
    </div>

    <script>
        function togglePasswordVisibility(id) {
            const passwordField = document.getElementById(id);
            const icon = document.querySelector(`#${id} + .show-password-icon`);
            if (passwordField.type === 'password') {
                passwordField.type = 'text';
                icon.textContent = '🔓'; // Unlock icon
            } else {
                passwordField.type = 'password';
                icon.textContent = '🔒'; // Lock icon
            }
        }
    </script>
</body>
</html>
 *@